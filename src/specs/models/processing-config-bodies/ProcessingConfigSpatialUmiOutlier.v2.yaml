title: Spatial UMI Outlier
type: object
description: Processing config body for the Visium HD spatial UMI local-outlier filter. Keys are sample UUIDs, values are per-sample configurations.
additionalProperties:
  type: object
  properties:
    enabled:
      type: boolean
    auto:
      type: boolean
    filterSettings:
      type: object
      properties:
        cutoff:
          type: number
        direction:
          type: string
          enum:
            - lower
            - upper
            - both
        binStep:
          type: number
  required:
    - filterSettings
