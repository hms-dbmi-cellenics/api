title: Patch sample file
description: Data for patching a sample file, can only be uploadStatus
type: object
properties:
  uploadStatus:
    type: string
  size:
    type: integer
additionalProperties: false