title: Samples by experiment
description: Samples entries separated by the experiment they belong to
type: array
items:
  type: object
  properties:
    experimentId:
      type: string
    projectUuid:
      type: string
    samples:
      $ref: ../api-body-schemas/Samples.v1.yaml
  required:
    - samples
    - experimentId
    - projectUuid
  additionalProperties: false