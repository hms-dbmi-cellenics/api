title: Update Samples Body
description: An array specifying updates to perform to a samples entry
type: object
properties:
  add:
    description: Array samples to set to the entry
    $ref: ../api-body-schemas/Samples.v1.yaml
  remove:
    description: An array of sample ids to delete from the entry
    type: array
    items:
      type: string